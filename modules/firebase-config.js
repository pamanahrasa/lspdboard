/**
 * firebase-config.js
 * ---------------------------------------------------------------------------
 * REQUIRED SETUP - this is the only file you need to edit to connect the
 * dashboard to your own Firebase project.
 *
 * Where to get these values:
 *   1. https://console.firebase.google.com -> create a project (free)
 *   2. Project settings (gear icon) -> General -> "Your apps" -> Add app -> Web (</>)
 *   3. Firebase shows you an object exactly like the one below - copy every
 *      value into the matching field here.
 *
 * Full step-by-step instructions (including Firestore + Authentication
 * setup, which you also need) are in README.md.
 * ---------------------------------------------------------------------------
 */

export const firebaseConfig = {
    apiKey: "PASTE_YOUR_API_KEY_HERE",
    authDomain: "PASTE_YOUR_AUTH_DOMAIN_HERE",
    projectId: "PASTE_YOUR_PROJECT_ID_HERE",
    storageBucket: "PASTE_YOUR_STORAGE_BUCKET_HERE",
    messagingSenderId: "PASTE_YOUR_MESSAGING_SENDER_ID_HERE",
    appId: "PASTE_YOUR_APP_ID_HERE"
};
